# ImagestoExcel
This tool uses Python to lock images inside Excel cells so they stay perfectly aligned and resize with the grid, rather than just floating on top.  Simply select your OneDrive photos in the browser, and it automatically crops and embeds them into a neat, professional column.
